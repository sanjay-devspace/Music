import 'package:flutter/material.dart';

class TuneHiveColors {
  // Primary Background
  static const Color charcoalBlack = Color(0xFF0F1419);

  // Secondary / Neutral
  static const Color silverGrey = Color(0xFF4B5563);

  // Primary Light Color
  static const Color coolWhite = Color(0xFFC9D1D9);

  // Main Accent
  static const Color electricBlue = Color(0xFF006BFF);

  // Text
  static const Color secondaryText = Color(0xFF9CA3AF);

  static const Color mutedText = Color(0xFF6B7280);

  // Surfaces
  static const Color cardSurface = Color(0xFF1A2029);

  static const Color elevatedSurface = Color(0xFF2A323D);
}
